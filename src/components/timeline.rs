use dioxus::prelude::*;

use crate::timeline_data::{TimelineData, TimelineEvent};

#[component]
pub fn Timeline(events: TimelineData) -> Element {
    todo!()
}

#[component]
fn Event(data: TimelineEvent) -> Element {
    todo!()
}